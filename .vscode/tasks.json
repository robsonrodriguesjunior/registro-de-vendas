{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "gradlew: clean compileJava -x webapp",
      "type": "shell",
      "command": "./gradlew",
      "args": ["clean", "compileJava", "-x", "webapp"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "dependsOn": "build",
      "problemMatcher": "$gradle"
    }
  ]
}
